# 需求文档 — Vue3 记账小工具

## 引言

### 项目背景

日常生活中，个人或家庭对收支情况的掌控意识逐渐增强。市面上已有随手记、鲨鱼记账、喵喵记账等多款记账产品，功能丰富但也较为复杂。本项目旨在基于 Vue3 开发一款轻量级记账小工具，聚焦核心记账场景，帮助用户快速记录每一笔收支，直观掌握资金流向。

### 项目目标

- 提供极简的收支记录体验，3 秒内完成一笔记账。
- 以图表形式直观展示收支趋势与分类占比。
- 数据本地持久化存储，保障离线可用与隐私安全。
- 响应式布局，兼顾桌面端与移动端使用。

### 项目范围

- 收支记录的增删改查
- 收支分类管理
- 统计报表（日/月/年）
- 数据导入导出
- 预算管理

---

## 功能需求

### 1. 记账录入

- 用户可快速录入一笔收入或支出，包含以下字段：
  - **金额**（必填，数字，支持小数点后两位）
  - **类型**（收入 / 支出）
  - **分类**（如：餐饮、交通、工资、理财等，支持自定义分类）
  - **账户**（如：现金、微信、支付宝、银行卡等）
  - **日期**（默认当天，可修改）
  - **备注**（选填，简短文字说明）
- 支持默认分类快速选择（常用分类置顶显示）。
- 录入完成后自动跳转至账单列表页。

### 2. 账单列表

- 按日期倒序展示所有收支记录。
- 每条记录显示：分类图标、分类名称、备注、金额（收入绿色，支出红色）。
- 按日分组，每组显示当日收入合计、支出合计。
- 支持按月份切换查看。
- 支持按分类、关键字（备注）筛选。
- 左滑或长按可删除记录，点击可编辑记录。

### 3. 分类管理

- 预置常用收支分类：
  - 支出：餐饮、交通、购物、居住、娱乐、医疗、教育、通讯、人情、其他
  - 收入：工资、奖金、理财、兼职、红包、其他
- 用户可新增自定义分类（含名称和图标选择）。
- 用户可编辑、删除自定义分类（预置分类不可删除，但可隐藏）。
- 分类支持排序，常用分类可置顶。

### 4. 账户管理

- 预置常见账户：现金、微信、支付宝、银行卡。
- 用户可新增自定义账户。
- 用户可编辑、删除自定义账户。
- 各账户显示当前余额（根据收支记录自动计算）。

### 5. 统计报表

- **月度统计**：
  - 当月总收入、总支出、结余。
  - 支出分类饼图 / 收入分类饼图。
  - 每日收支折线图。
- **年度统计**：
  - 年度收支趋势柱状图（按月）。
  - 年度收支分类排名。
- 支持按自定义时间范围筛选统计。
- 点击分类可下钻查看该分类下所有明细。

### 6. 预算管理

- 用户可设置月度总预算。
- 用户可设置分类预算（如：餐饮预算 2000 元/月）。
- 首页 / 统计页显示预算使用进度条，超出预算时红色预警。
- 预算周期按自然月自动重置。

### 7. 数据管理

- 支持将所有账单数据导出为 CSV / JSON 文件。
- 支持从 CSV / JSON 文件导入数据（格式校验 + 冲突提示）。
- 数据全部存储于浏览器本地（IndexedDB / localStorage），无需联网。

---

## 非功能需求

### 1. 性能

- 页面首次加载时间不超过 2 秒。
- 记账录入到保存的响应时间不超过 500ms。
- 支持万级账单数据量下的流畅滚动与检索。

### 2. 安全性

- 所有数据本地存储，不上传任何服务器。
- 数据导出文件不含加密信息，但导入时做格式校验防止注入。

### 3. 可用性

- 界面简洁直观，新用户无需引导即可完成首次记账。
- 移动端与桌面端均可正常使用。
- 离线状态下所有核心功能可用。

### 4. 可维护性

- 组件化开发，单一职责。
- TypeScript 类型覆盖率达 90% 以上。
- 核心业务逻辑与 UI 组件分离。

---

## 用户故事

- **作为一名普通用户，我希望打开应用就能快速记一笔账，不需要繁琐的步骤。**
- **作为一名普通用户，我希望看到每个月钱花在了哪里，哪些分类花费最多。**
- **作为一名普通用户，我希望设置月度预算来控制开支，并在超支时得到提醒。**
- **作为一名普通用户，我希望按分类和关键字搜索历史账单，方便回查。**
- **作为一名普通用户，我希望我的财务数据只保存在本地，不用担心隐私泄露。**
- **作为一名普通用户，我希望导出账单数据备份，也能从备份中恢复。**
- **作为一名多账户用户，我希望分别查看每个账户的余额和流水。**

---

## 用例

### 用例1：快速记一笔支出

- **参与者**：用户
- **前置条件**：用户已打开应用
- **主要步骤**：
  1. 用户点击首页"记一笔"按钮。
  2. 系统弹出记账面板，默认为"支出"类型。
  3. 用户输入金额，选择分类（如"餐饮"）。
  4. （可选）选择账户、修改日期、填写备注。
  5. 用户点击"保存"。
  6. 系统保存记录，刷新账单列表，显示操作成功提示。
- **后置条件**：一笔新支出记录已创建，对应账户余额减少。

### 用例2：编辑已有记录

- **参与者**：用户
- **前置条件**：账单列表中存在至少一条记录
- **主要步骤**：
  1. 用户在账单列表中点击某条记录。
  2. 系统打开编辑面板，回填该记录的所有信息。
  3. 用户修改需要变更的字段。
  4. 用户点击"保存"。
  5. 系统更新记录，刷新列表。
- **后置条件**：记录信息已更新，相关账户余额重新计算。

### 用例3：查看月度统计

- **参与者**：用户
- **前置条件**：当月存在收支记录
- **主要步骤**：
  1. 用户切换至"统计"页面。
  2. 系统默认展示当月统计数据：总支出、总收入、结余。
  3. 系统展示支出分类饼图与每日收支折线图。
  4. 用户可点击饼图中某一分类，查看该分类下所有明细。
  5. 用户可切换月份查看历史统计。
- **后置条件**：无数据变更。

### 用例4：设置月度预算

- **参与者**：用户
- **前置条件**：用户已打开应用
- **主要步骤**：
  1. 用户进入"预算"页面。
  2. 用户点击"设置总预算"，输入月度总预算金额并保存。
  3. （可选）用户为某一分类设置分类预算。
  4. 系统显示当前预算使用进度。
- **后置条件**：预算设置已保存，后续记账时实时更新进度。

### 用例5：导出数据

- **参与者**：用户
- **前置条件**：系统中存在账单数据
- **主要步骤**：
  1. 用户进入"设置"页面。
  2. 用户点击"导出数据"，选择导出格式（CSV / JSON）。
  3. 系统生成文件并触发浏览器下载。
- **后置条件**：用户本地获得数据备份文件。

---

## 技术要求

| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 前端框架 | Vue 3 + Composition API | 使用 `<script setup>` 语法 |
| 构建工具 | Vite | 快速开发与构建 |
| 语言 | TypeScript | 类型安全 |
| 状态管理 | Pinia | 轻量、支持 Composition API |
| 路由 | Vue Router 4 | SPA 路由管理 |
| UI 框架 | Vant 4（移动端优先）/ Element Plus（桌面端适配） | 按需引入 |
| 图表库 | ECharts | 饼图、折线图、柱状图 |
| 本地存储 | IndexedDB（Dexie.js） | 大容量结构化本地存储 |
| CSS 方案 | Tailwind CSS | 原子化 CSS，快速布局 |
| 代码规范 | ESLint + Prettier | 统一代码风格 |
| 版本控制 | Git | 分支管理 |
